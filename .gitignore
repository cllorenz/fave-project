atomic_predicates_verifier_2018410.zip
Pyrex-0.9.9.tar.gz
Pyrex-0.9.9
pybison

# Third-party benchmark source archives, kept locally and never committed:
# together they are ~10 GB, and the workloads vendor only what they read
# (fave/bench/wl_cloud/cloud-tf/, the two only-inserts traces). See
# CLOUD_BENCH_PLAN.md for what is in them and why the rest stays out.
cloud_bench.tar.bz2
deltanet-NSDI17-dataset.tar.gz
nsdi-data-sets/

# Python / test artifacts
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.venv/
venv/
fave/results/

.yolobox.toml
