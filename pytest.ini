[pytest]
# Never collect tests from deprecated/ directories anywhere in the tree.
# Mirrors the lint convention (fave/test/lint_test.sh ignores deprecated/*).
norecursedirs = *deprecated* .git build dist *.egg-info
